# Browser requests use this prefix. Keep /api/v1 when the Vite proxy is used.
VITE_TENDER_REVIEW_API_BASE=/api/v1

# Vite development proxy target. This value is not exposed to browser code.
TENDER_REVIEW_BACKEND_URL=http://127.0.0.1:8000
